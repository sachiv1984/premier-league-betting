name: Update Premier League Betting Analysis

on:
  schedule:
    - cron: '0 8 * * *'  # Daily at 08:00 UTC
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  update-analysis:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🗂️ Create public directory if missing
        run: mkdir -p ./public

      - name: 🌐 Setup Chrome for Puppeteer
        uses: browser-actions/setup-chrome@latest

      - name: 🧹 Clear previous fixtures cache (optional)
        run: rm -f ./fixtures.json ./matchweeks.json || true

      - name: 🔄 Run scraping script to fetch all fixtures & build matchweeks
        run: |
          echo "Running scraping of all Pre
